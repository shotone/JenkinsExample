pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Build') {
            steps{
                echo 'Jobs is Called by User: '
                sh 'whoami'
                sh 'ls -l /home/ | grep "zip"'
            }

        }

    }
}
