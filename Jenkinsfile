pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Build') {
            steps{
                echo 'Committed successfully'
            }

        }

        stage('Build'){
            steps{
                sh 'npm install'
            }
        }
    }
}
